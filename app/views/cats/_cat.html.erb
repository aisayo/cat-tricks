<h2><%= @cat.name %></h2>

<p>Color: <%= @cat.color %></p>

<% if @cat.tricks.count > 0 %>
  <ul>
    <% @cat.tricks.each do |trick| %>
      <li><%= trick.name %></li>
    <% end %>
  </ul>
<% else %>
  Your cat doesn't know anything yet!
<% end %>
